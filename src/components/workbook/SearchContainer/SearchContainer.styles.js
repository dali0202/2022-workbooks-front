import styled from "styled-components";

const SearchWrap = styled.div`
  display: flex;
  align-items: center;
  background-color: #ff7575;
  border-radius: 0.4rem;
  box-shadow: 0 0.2rem 0.8rem #ff7c7c, 0 0 0 0.4rem #ffffffeb;
  width: 11rem;
  padding: 0.7rem;
  transform: scale(0.9);
`;
const Input = styled.input``;

export { SearchWrap, Input };
